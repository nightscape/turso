# This file contains settings for `cargo hakari`.
# See https://docs.rs/cargo-hakari/latest/cargo_hakari/config for a full list of options.

hakari-package = "workspace-hack"

# Format version for hakari's output. Version 4 requires cargo-hakari 0.9.22 or above.
dep-format-version = "4"

# Setting workspace.resolver = "2" or higher in the root Cargo.toml is HIGHLY recommended.
# Hakari works much better with the v2 resolver. (The v2 and v3 resolvers are identical from
# hakari's perspective, so you're welcome to set either.)
#
# For more about the new feature resolver, see:
# https://blog.rust-lang.org/2021/03/25/Rust-1.51.0.html#cargos-new-feature-resolver
resolver = "2"

# Add triples corresponding to platforms commonly used by developers here.
# https://doc.rust-lang.org/rustc/platform-support.html
platforms = [
    "x86_64-unknown-linux-gnu",
    "aarch64-apple-darwin",
    "x86_64-pc-windows-msvc",
]

# Write out exact versions rather than a semver range. (Defaults to false.)
# exact-versions = true

# Omitted from workspace-hack's own dependency list (still traversed, so
# feature unification for everything below them is unchanged). Each of these
# is unbuildable on wasm32-unknown-unknown — tokio's "full" pulls `mio`,
# hyper/hyper-util/tokio-rustls pull it transitively, and rusqlite "bundled"
# needs a C toolchain — and workspace-hack is a dependency of turso_core, so
# an ungated entry here breaks every wasm consumer of the fork. Crates that
# genuinely need them declare them directly.
[final-excludes]
third-party = [
    { name = "tokio" },
    { name = "hyper" },
    { name = "hyper-util" },
    { name = "tokio-rustls" },
    { name = "rusqlite" },
]
